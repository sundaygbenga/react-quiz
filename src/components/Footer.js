function Footer({ children }) {
	return (
		<footer
		// style={{
		// 	display: "grid",
		// 	gridTemplateColumns: "repeat(1fr, 1fr, 1fr)",
		// 	// justifyContent: "space-between",
		// }}
		>
			{children}
		</footer>
	);
}

export default Footer;
